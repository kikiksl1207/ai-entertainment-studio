# Revised story manuscripts: 2026-09-25

## Source status

The final ZIPs in `E:\LuminaStage_Upload\문체분리_최종본_20260925` are new source versions, not live-site updates. The reader manuscript bytes in each ZIP match the corresponding files under `E:\Codex\Stories\두_작품_문체분리_최종본`.

| Story | Main + extra parts | Reader text characters | Narrative structure | Image prompts |
| --- | ---: | ---: | --- | ---: |
| 내 이름을 먹지 않은 괴물 | 28 + 4 | 205,934 | Yun Hae-won's first person; 124 `* * *` transitions | 146 |
| 우리는 서로의 몸에 반역을 썼다 | 40 + 4 | 285,213 | Limited third person; 189 Roman-numeral sections | 235 |

The independently counted part totals and character lengths match the supplied handoff. The two narrators and sectioning conventions are visibly distinct. This does not prove that readers will perceive the prose as human-written; editorial review remains necessary.

All 32 and 44 part headings respectively match the prior packages. The reader manuscript bytes changed, but each image-prompt file is byte-for-byte identical to its previous package. Those prompts may be reusable after their placement is reviewed against the new text; identical prompt files alone do not establish scene alignment.

## Publication boundary

Both stories are already published from earlier source checksums. The current fixed-route intake recognizes those exact approved hashes and treats an existing slug as already published. It also maps `[장면 N]` markers to scene beats, while the revised manuscripts use different section markers. Replacing only the prose would invalidate source/release checksums, scene-to-image mapping, author style anchors, and any generated-route references. Do not overwrite the active release or copy these ZIPs into the public asset directory.

Publication needs an explicit new immutable manuscript/release version, a mapping from each revised part and section to scene beats, reviewed image prompts and existing generated routes, an author final review, and a controlled active-release switch with rollback. Preserve the old version for existing readers until the progress migration rule is decided.

The additional local manuscript `E:\Codex\Stories\살인자는_죽은_자의_능력을_계승한다\최종본` is not in the public story catalog. It has not been imported or counted as a published story.

## Author credit

The public catalog and detail API now carry an optional display author, independent of the account that uploads the work. The four explicitly identified company-produced published slugs backfill to `루미나`; future approved company imports set that credit and include it in search. External-author works must receive their contractual pen name, not a blanket `루미나` default.
